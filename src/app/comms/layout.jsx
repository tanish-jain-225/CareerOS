export const metadata = {
  title: 'CareerOS | Communication CRM',
  description: 'Outreach cadence management and propagation monitoring engine.',
};

export default function CommsLayout({ children }) {
  return children;
}
