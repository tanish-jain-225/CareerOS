export const metadata = {
  title: 'CareerOS | Identity Dossier',
  description: 'Verified proof of work and professional manifestation manifest.',
};

export default function IdentityLayout({ children }) {
  return children;
}
