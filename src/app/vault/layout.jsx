export const metadata = {
  title: 'CareerOS | Document Vault',
  description: 'Encrypted storage node for mission-critical credentials and artifacts.',
};

export default function VaultLayout({ children }) {
  return children;
}
