export const metadata = {
  title: 'CareerOS | Infiltration Hub',
  description: 'Real-time target tracking and CRM for high-stakes career progression.',
};

export default function JobsLayout({ children }) {
  return children;
}
