export const metadata = {
  title: 'CareerOS | Tactical Prep Hub',
  description: 'Zero-fault interview engineering and manifestation system.',
};

export default function PrepLayout({ children }) {
  return children;
}
